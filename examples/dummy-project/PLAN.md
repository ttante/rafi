# Dummy project for foreman e2e test

## Steps
1. Create a file `hello.txt` containing the text "step 1 done"
2. Create a file `world.txt` containing the text "step 2 done"
3. Create a file `summary.md` containing a one-line summary of what was created
