# Existing export work

## BASE-CSV-EXPORT

`LIVE-CSV-EXPORT` is the existing next ticket. It exports the complete task
list to CSV with task text and completion state. Preserve its stable ticket ID
when later requirements refine it.
